# API root; auth endpoints are resolved as /auth/* below this path.
# Configure this per deployment; the local .env is intentionally gitignored.
API_BASE_URL=https://api.example.com/api/v1
ENABLE_FCM=false
